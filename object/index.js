// DEPTH

// singleton -> read more about it from internet

// if we create object using constructor then it will be singleton

// PENDING LIST [ SYMBOL, ]

const user1 = {
  firstName: "Koushik",
  lastName: "Chowdhury",
  city: "Malda",
  state: "West Bengal"
}

console.log(Object.keys(user1));
console.log(Object.values(user1));
